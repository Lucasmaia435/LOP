//Lucas Maia 27/09/2018
//function abs(a, b) {
//	return Math.abs(a - b);
//}
//function pow(a, b){
//	return Math.pow(a, b);
//}
//function sqrt(a){
//	return Math.sqrt(a);
//}
/*	a = pow(abs(x - x1),2);
	b = pow(abs(y - y1),2);
	d = sqrt(a+b);
	console.log(d) */

//function dist()


/*hit = collideRectRect(x,y,char.width,char.height,x2,y2,chip.width,chip.height); // colisão rolando normalmente <3 / FAZER UMA FUNÇÃO PARA COLISÃO X-LATERAL
	if (hit == true){		
		console.log("collide ")
		if(x + char.width >= x2){
			x += 7;
		} 
		if(x <= x2 + chip.width){
			x -= 7;
		}
		if(y + char.height >= y2){
			y -= 7;
		}
		if( y <= y2 + chip.height)
			y += 7;
		}
	console.log(x + " " + y)
*/
